require 'spec_helper'

describe JenkinsStatsd do
  it 'has a version number' do
    expect(JenkinsStatsd::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
